# 城会玩2.0 云服务迁移说明

## 背景
微信云开发服务到期，项目从云开发模式迁移到免费 CDN + 本地存储模式。

## 改动总结

### 1. 静态图片 → jsDelivr CDN
- **省份风景图**（34张）和**角色卡插画**（34张）从云存储迁移到 GitHub + jsDelivr CDN
- 原始图片 104.8 MB，压缩后 6 MB（缩减 94.3%）
- 压缩后图片位于 `cloud-images/compressed/` 目录
- 代码中通过 `utils/image-config.js` 统一管理 CDN URL
- **需要操作**：创建 GitHub 仓库，上传压缩后的图片，更新 `image-config.js` 中的 CDN_BASE

### 2. 用户照片 → 本地文件系统
- 旅行照片、美食照片、头像从 `wx.cloud.uploadFile` 改为 `wx.getFileSystemManager().saveFile()`
- 照片保存在用户设备的 `wx.env.USER_DATA_PATH`（200MB 额度）
- 每个用户的照片只存在自己设备上，换设备不同步
- 涉及文件：`upload.js`、`city-detail.js`、`profile.js`

### 3. 登录 → 本地 UUID
- 不再调用 `login` 云函数，直接生成本地 UUID 作为用户标识
- 格式：`local_{timestamp}_{random}`
- 首次打开自动登录，无需用户操作

### 4. 数据同步 → 纯本地
- `syncFromCloud` 和 `syncToCloud` 在 `useCloud=false` 下自动跳过
- 所有数据存储在 `wx.setStorageSync`，不依赖云端

### 5. 群组功能 → 降级模式（未禁用）
- 群组页面保留云连接尝试，连不上自动降级到本地模式
- **可用功能**：创建本地群组、查看自己的打卡数据
- **不可用功能**：邀请好友加入、照片共享、查看他人数据
- 用户尝试不可用功能时会看到明确提示

### 6. 界面图标 → 无需改动
- `images/` 文件夹中的所有 UI 图片（头图、tab图标、地图标记、徽章等）
- 已经打包在小程序内，不走云存储，不受影响

## 需要完成的步骤

### 步骤1：补齐缺失图片
从微信云开发控制台下载：
- `cities/macau.png`（澳门风景图）
- `cities/hebei.png`（重新下载，现有文件太小）
放入 `cloud-images/cities/` 后重新运行压缩

### 步骤2：创建 GitHub 仓库
1. 在 GitHub 创建一个公开仓库，例如 `chenghuiwan-assets`
2. 在仓库中创建 `cities/` 和 `cards/` 两个文件夹
3. 把 `cloud-images/compressed/cities/` 的所有 .jpg 文件上传到 `cities/`
4. 把 `cloud-images/compressed/cards/` 的所有 .jpg 文件上传到 `cards/`

### 步骤3：更新 CDN 地址
编辑 `utils/image-config.js`，把：
```javascript
var CDN_BASE = 'https://cdn.jsdelivr.net/gh/USERNAME/REPO@main';
```
替换为你的实际地址，例如：
```javascript
var CDN_BASE = 'https://cdn.jsdelivr.net/gh/yourname/chenghuiwan-assets@main';
```

### 步骤4：测试
在微信开发者工具中：
1. 编译运行，检查省份图片和角色卡图片是否正常显示
2. 测试城市打卡、照片上传、相册功能
3. 测试群组页面（应显示降级提示）

## 功能对照表

| 功能 | 改造前 | 改造后 |
|-----|--------|--------|
| 地图点亮城市 | ✅ | ✅ |
| 城市搜索 | ✅ | ✅ |
| 城市详情/省份详情 | ✅ | ✅ |
| 角色卡收集/抽卡 | ✅ | ✅ |
| 照片上传/相册 | ✅ 云存储 | ✅ 本地存储 |
| 笔记/避坑指南 | ✅ | ✅ |
| 成就系统 | ✅ | ✅ |
| 跨设备数据同步 | ✅ | ❌ |
| 群组创建 | ✅ | ✅ 本地 |
| 群组加入/照片共享 | ✅ | ❌ 需云服务 |
| 内容安全审核 | ✅ | ❌ 已跳过 |

## 如果想恢复完整群组功能

### 选项A：续费微信云开发（19.9元/月）
最简单的方式。把 `app.js` 中 `useCloud` 改回 `true`，重新上传云函数即可。

### 选项B：使用免费 BaaS 服务
如 LeanCloud（leancloud.cn），有免费额度，支持微信小程序，国内可访问。需要较大代码改造。

### 选项C：周期性免费环境
注销当前云环境 → 等待1个月 → 创建新免费环境（已发布小程序限15天）。适合临时使用。
