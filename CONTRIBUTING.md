# 贡献指南

欢迎为城会玩 2.0 提交贡献！无论是报告问题、提出建议还是提交代码，都非常感谢。

## 报告问题

如果发现 Bug 或有功能建议，请通过 [GitHub Issues](https://github.com/tl66666/chw2.0/issues) 提交。

提交 Issue 时请包含以下信息：

- 问题描述：发生了什么，期望发生什么。
- 复现步骤：从哪个页面进入，点击了什么按钮。
- 设备信息：微信版本、手机型号。
- 截图：如有请附上。

## 提交代码

### 开发环境

- 微信开发者工具（最新稳定版）
- 已开通微信云开发环境

### 提交流程

1. Fork 本仓库。
2. 创建功能分支：`git checkout -b feature/your-feature-name`。
3. 提交修改：`git commit -m "feat: 简要描述修改内容"`。
4. 推送到分支：`git push origin feature/your-feature-name`。
5. 提交 Pull Request，描述修改内容和目的。

### Commit 规范

| 前缀 | 用途 |
| --- | --- |
| `feat` | 新功能 |
| `fix` | Bug 修复 |
| `docs` | 文档修改 |
| `style` | 代码格式调整（不影响功能） |
| `refactor` | 代码重构 |
| `perf` | 性能优化 |
| `chore` | 构建、工具链等杂项 |

示例：`feat: 新增相册长按删除照片功能`

### 代码规范

- JavaScript 使用 ES5 语法（微信小程序兼容性要求）。
- 缩进使用 2 个空格。
- 变量命名使用驼峰式（camelCase）。
- WXML 属性换行对齐。
- WXSS 类名使用 kebab-case。
- 新增页面需在 `app.json` 或对应分包的 `pages` 数组中注册。
- 云函数修改后需在微信开发者工具中重新上传部署。

### 测试要点

提交 PR 前请确认：

- 小程序能正常编译，无控制台报错。
- 新增功能在真机上测试通过（不仅限模拟器）。
- 涉及云函数的修改已上传部署并验证。
- 删除类操作不会影响其他用户的数据。
- 数据同步逻辑正确，删除后重新进入不会恢复。

## 目录结构说明

详细目录结构请参考 [README.md](./README.md#仓库结构) 中的"仓库结构"章节。

## 云函数修改

修改云函数后：

1. 在微信开发者工具中右键对应云函数文件夹。
2. 选择"上传并部署：云端安装依赖"。
3. 等待部署完成后进行测试。

## 项目展示页

项目展示页（`index.html`）部署在 GitHub Pages，修改后推送到 `main` 分支即可自动更新。

展示页地址：[https://tl66666.github.io/chw2.0/](https://tl66666.github.io/chw2.0/)

## 联系作者

如有疑问，可通过 GitHub Issue 或 Pull Request 与作者联系。
